import './bootstrap';
import {
    Sidenav,
    Ripple,
    Offcanvas,
    initTE,
    Input,
    Datetimepicker,
    Modal,
    Collapse,
} from "tw-elements";

initTE({ Sidenav, Ripple, Offcanvas, Collapse, Input, Datetimepicker, Modal })